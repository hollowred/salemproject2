<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <link rel="stylesheet" href="/css/style.css">
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
      <h1> All of Our Favorite Animals </h1>
      <nav>
        <form action="/animals/new" method="GET">
          <button type="submit" href="/animals/new">Add more animals</button>
        </form>
      </nav>
      <ul>
           <% for (let i = 0; i < data.length; i++) { %>
               <li>
                 <a href="/animals/<%=data[i]._id%>">
                    <h3><%=data[i].name%></h3>
                    <img src="<%=data[i].img%>">
                   </a>
                <div class="buttons">
                  <form action="/animals/<%=data[i]._id%>/edit" method="GET">
                    <button type="submit" href="/animals/<%=data[i]._id%>/edit" > EDIT</button>
                  </form>
                  <form action="/animals/<%=data[i].id; %>?_method=DELETE" method="POST">
                   <input type="submit" value="DELETE"/>
                 </form>
               </div>
               </li>
           <% } %>
       </ul>
  </body>
</html>
